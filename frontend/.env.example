# Base URL of the donesi.me Express API.
# Must match the backend PORT (defaults to 5001).
# Exposed to the browser — never put secrets in NEXT_PUBLIC_* variables.
NEXT_PUBLIC_API_URL="http://localhost:5001"
